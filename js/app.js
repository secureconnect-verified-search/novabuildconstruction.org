document.addEventListener('DOMContentLoaded',()=>{const observer=new IntersectionObserver(items=>items.forEach(i=>{if(i.isIntersecting){i.target.classList.add('visible');observer.unobserve(i.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const top=document.querySelector('#toTop');if(top){addEventListener('scroll',()=>top.hidden=scrollY<600);top.onclick=()=>scrollTo({top:0,behavior:'smooth'})}document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();const b=f.querySelector('button[type=submit]');if(b){b.textContent='Message received — thank you';b.disabled=true}}));});

document.addEventListener('DOMContentLoaded',()=>{const o=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,end=+el.dataset.count,duration=1200,start=performance.now();const tick=t=>{const n=Math.min((t-start)/duration,1);el.textContent=Math.floor(n*end).toLocaleString()+ (el.dataset.suffix||'');if(n<1)requestAnimationFrame(tick)};requestAnimationFrame(tick);o.unobserve(el)}),{threshold:.5});document.querySelectorAll('[data-count]').forEach(e=>o.observe(e));});

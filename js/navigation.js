document.addEventListener('DOMContentLoaded',()=>{const nav=document.querySelector('.site-nav'),toggle=document.querySelector('.menu-toggle'),links=document.querySelector('.nav-links');const setNav=()=>nav.classList.toggle('scrolled',scrollY>30);setNav();addEventListener('scroll',setNav);toggle?.addEventListener('click',()=>links.classList.toggle('open'));document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')));});
